#
# Dummy table for the generic db-api
#


CREATE TABLE test (
  id         INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
  string     varchar(100)
);

CREATE TABLE test2 (
  id         INT NOT NULL,
  string     varchar(100)
);
